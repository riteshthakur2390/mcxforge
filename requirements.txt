# =============================================================
# SignalForge — Python Dependencies
# =============================================================
# Install inside Docker (handled by Dockerfile automatically)
#
# If running locally outside Docker:
#   pip install -r requirements.txt
#
# Note: TA-Lib requires system library first:
#   Mac:   brew install ta-lib
#   Linux: see Dockerfile for compile-from-source steps
# =============================================================

# ── DATA SOURCES (FREE — no account needed) ─────────────────
yfinance>=0.2.40          # Yahoo Finance — NIFTY historical data, free
pyotp>=2.9.0               # TOTP generation for Groww auth

# ── ZERODHA KITE ─────────────────────────────────────────────
kiteconnect>=4.2.0

# ── Groww ─────────────────────────────────────────────
# growwapi>=1.5.0
pyotp>=2.9.0
pandas>=2.1.0,<3.0.0

# ── UPSTOX ─────────────────────────────────────────────
kiteconnect>=4.2.0
upstox-python-sdk>=2.0.0

# ── DHAN ───────────────────────────────────────────────
dhanhq>=2.2.0

# ── DATA & NUMERICS ──────────────────────────────────────────
numpy==1.26.4
scipy>=1.11.0
pyarrow>=14.0.0              # fast parquet read/write for data cache


# ── TECHNICAL INDICATORS ─────────────────────────────────────
# pandas-ta>=0.3.14b0          # RSI, MACD, ADX, SuperTrend, BB, Stoch, PSAR
# TA-Lib>=0.4.28               # compiled from source in Dockerfile
# pip install git+https://github.com/twopirllc/pandas-ta.git@development
# Install the system C library via Homebrew
# brew install ta-lib
# Then install the Python wrapper
# pip install TA-Lib
# Optional: TA-Lib (advanced indicators)
# Mac:
#   brew install ta-lib
#   pip install TA-Lib
# Recommended alternative (stable, optional)
ta>=0.11.0


# ── ML / CLASSIFIERS ─────────────────────────────────────────
scikit-learn>=1.4.0          # RandomForest, TimeSeriesSplit, metrics
xgboost>=2.0.0               # primary ML model (40% weight)
lightgbm>=4.2.0              # secondary ML model (40% weight)
joblib>=1.3.0                # model save/load (.pkl files)


# ── ASYNC / WEB ──────────────────────────────────────────────
flask>=3.0.0                 # dashboard web server
flask-socketio>=5.3.6        # real-time WebSocket push to browser
httpx>=0.27.0                # async HTTP for LLM API calls
requests>=2.31.0             # Telegram alerts (sync)
websocket-client>=1.7.0      # Kite WebSocket ticker (future use)


# ── UTILITIES ─────────────────────────────────────────────────
python-dotenv>=1.0.1         # loads .env file
loguru>=0.7.2                # structured logging with rotation
pytz>=2024.1                 # IST timezone handling
python-dateutil>=2.9.0       # date parsing utilities
six==1.17.0

# ── COMPATIBILITY FIX (IMPORTANT) ─────────────────────────────
# Required for pandas_ta patched version (pkg_resources support)
setuptools>=70.0.0

# ── ANTHROPIC SDK ─────────────────────────────────────────────
# For LLM signal rationale, morning outlook, EOD analysis
# Optional — system works without this
anthropic>=0.25.0


# ── TESTING ──────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0       # async test support
